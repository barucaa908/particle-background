# 发布上架清单

粒子背景发布到 **GitHub**、**Chrome 网上应用店** 和 **Edge 加载项** 的完整步骤。
涉及账号登录、付费、人工审核的部分必须由你自己完成，我已把能自动化的都备好了。

---

## 1. GitHub（开源仓库，免费）

### 1.1 准备（我已自动完成）

- [x] 本地 git 仓库已初始化（`git init` + 首次提交）
- [x] `LICENSE`（MIT）、`.gitignore` 已就位
- [x] `publish-github.ps1` 一键发布脚本
- [ ] **Git for Windows 安装**（winget 后台安装中，装完需重新打开终端让 PATH 生效）

### 1.2 登录 GitHub（需要你操作，约 3 分钟）

**方式 A：GitHub CLI（推荐）**

```powershell
winget install --id GitHub.cli -e
gh auth login        # 选 GitHub.com → HTTPS → 用浏览器登录
```

**方式 B：个人访问令牌（PAT）**

1. 打开 https://github.com/settings/tokens → Generate new token (classic)
2. 勾选 `repo` 权限，生成形如 `ghp_xxxx` 的令牌

### 1.3 发布

```powershell
# 方式 A：已登录 gh
.\publish-github.ps1

# 方式 B：有令牌
$env:GH_TOKEN = "ghp_你的令牌"
.\publish-github.ps1
```

仓库名默认 `particle-background`（公开），可用参数改：`.\publish-github.ps1 -RepoName xxx -Visibility private`

### 1.4 仓库设置建议

- **Description**：`Interactive constellation particle background — browser extension & DeepSeek Harness GUI beautifier. 星座连线粒子背景。`
- **Topics**：`particle-background`、`chrome-extension`、`edge-extension`、`canvas`、`browser-extension`、`deepseek-harness`
- 发布 Release：GitHub → Releases → 上传 `release/particle-background-v1.0.0.zip`，勾选"从 zip 自动生成安装包"（Chrome 商店 / Edge 商店也可引用此 zip 的下载链接）

---

## 2. Chrome 网上应用店（谷歌扩展仓库，$5 一次性注册费）——本次跳过

> ⚠️ 需要 $5 注册费，按你的要求本次不做。以后想上架随时可以补，素材和文案都已备好。
> 如需上架：登录 Google 账号 → https://chrome.google.com/webstore/devconsole → 支付 $5 →
> 添加新项目上传 `release/particle-background-v1.0.0.zip` → 填下方文案与 `release/screenshots/` 截图 → 提交审核。

> **替代方案（免费）**：直接把 zip 分享给别人，拖进 `chrome://extensions`（开发者模式）即可安装；
> 或把 zip 挂到 GitHub Releases 里供下载。

---

## 3. Edge 加载项（微软扩展，免费，无需付费）

> 需要你登录 Microsoft 账号，我无法代为操作。

### 3.1 注册开发者

1. 打开 https://partner.microsoft.com/dashboard/microsoftedge
2. Microsoft 账号登录 → 完成开发者注册（**免费**，需填写基本信息）
3. 若是企业：创建"发布者档案"（个人开发者走个人流程）

### 3.2 提交扩展

1. **创建新提交** → 上传 `release/particle-background-v1.0.0.zip`
2. 填写列表（同 Chrome 文案）：
   - 类别：**视觉样式（Visual styles）/ 外观**（Edge 有专门的外观分类）
   - 截图：`release/screenshots/` 三张
   - 隐私声明：无数据收集（README 已含权限说明）
3. 提交审核 → 通常 2~5 个工作日

> 提示：Edge 加载项审核较慢，且首版常需补充截图/说明，按提示补交即可。
> Firefox AMO（addons.mozilla.org）同样可提交，manifest 已带 gecko id。

---

## 商店文案（复制即用）

### 名称
- 中文：**粒子背景 — 星座连线 · 星云 · 流星**
- English：**Particle Background — Constellation · Nebula · Shooting Stars**

### 简短描述（135 字符内）
- 中文：给任何网页加一层会呼吸的星空：星座连线粒子、星云光晕、流星与鼠标交互，主题自适应。
- EN：A living starfield for any website — constellation links, nebula glow, shooting stars & mouse interaction. Theme aware, safe on every site.

### 完整描述（中文）

**粒子背景** 为任意网页添加一层动态星空氛围：

- **星座连线**：粒子缓慢漂移，彼此靠近时自动连出细线，透明度随距离渐隐
- **星云光晕**：品牌蓝与紫的光晕缓慢流动，增加色彩深度
- **星尘与流星**：细小星点闪烁，偶尔有流星斜向划过
- **鼠标交互**：光标像引力中心，附近粒子被吸引并向光标延伸光弧

**特性**

- 主题自适应：自动读取页面明暗，深色浅色都恰到好处
- 两种渲染模式：背景模式（粒子藏在内容之下）与浮层模式（对任何网站都安全）
- 精细控制：密度、五种配色（蓝/紫/青/白/绿）、各特效独立开关
- 逐站停用 + 设置云同步
- 隐私友好：无网络请求、无数据收集，仅申请 storage 权限
- 轻量：零依赖、DPR 自适应、尊重系统"减弱动态效果"设置

**使用**：安装后打开任意网页即生效；点击工具栏图标可调设置、按网站停用。

### Full description (English)

**Particle Background** adds a living starfield atmosphere to any webpage:

- **Constellation network**: particles drift slowly and link with thin fading lines when close
- **Nebula glow**: deep blue & violet glows slowly flow for extra depth
- **Star dust & shooting stars**: tiny twinkling dots, with occasional meteors streaking across
- **Mouse interaction**: the cursor acts like a gravity well — nearby particles drift toward it and draw glowing arcs

**Features**

- Theme aware: detects dark/light pages automatically
- Two render modes: Background (behind the content) and Overlay (safe on any site)
- Fine controls: density, 5 color palettes (blue / violet / cyan / white / green), per-effect toggles
- Per-site disable + cloud-synced settings
- Privacy friendly: no network requests, no data collection, only the `storage` permission
- Lightweight: zero dependencies, DPR-aware, respects prefers-reduced-motion

**Usage**: works on any page right after install; click the toolbar icon to tweak settings or disable per-site.

---

## 素材清单（全部已就绪）

| 素材 | 位置 |
| --- | --- |
| 扩展安装包 zip | `release/particle-background-v1.0.0.zip` |
| 商店截图 ×3（1280×800） | `release/screenshots/` |
| 图标 16/32/48/128 | `extension/icons/` |
| 权限/隐私说明 | `extension/README.md` |
| GitHub 发布脚本 | `publish-github.ps1` |

## 时间线预估

| 步骤 | 耗时 | 需要你 |
| --- | --- | --- |
| GitHub 仓库 + 源码 | 5 分钟 | GitHub 账号登录（gh auth login，免费） |
| Edge 加载项 | 30 分钟 + 审核 2~5 天 | Microsoft 账号（免费） |
| Chrome 网上应用店 | 跳过（需 $5 注册费） | — |
